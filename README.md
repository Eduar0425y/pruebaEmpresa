para instalar next : en la terminal npm install next react react-dom
abrir package.json:
    "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint"
    }

instalar axios:  npm install axios